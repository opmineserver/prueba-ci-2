sudo: false

services:
  - mysql
  - postgresql

cache:
  directories:
   - $HOME/.m2
   - ./front-end/node_modules


jobs:
  include:
   - stage: Front-End
     language: node_js
     node_js: node
     
     before_script:
      - npm install -g @angular/cli
      
     script:
      - cd front-end
      - npm install
      - ls -a
      - ng build --prod
     script:
      - echo ===============================